ElasticWolf is an application for managing Amazon Elastic Cloud servides in simple and 
easy to use UI. 

This project is a fork of ElasticFox with support for VPCs, GovCloud and packaged with 
all necessary tools and utilities to deal with private and public keys and SSL certificates.

Windows version of the tool is packaged with openssl for generating keys and ssh clients for 
accessing Linux instances.

The project was sponsored by Amazon to deal with new GovCloud services which are not 
supported by other tool.

Developing

 Primary developemnt is done on Mac so there is special dev mode to run it as 
 OS X application with symlink to actual source code

   make dev

 will make links and enables OS X app ready run. To launch execute 

   make run

Building

 To create binary packages for Mac and Windows just type

  make build

 it will produce to .zip files for each platform

Developers:
  Vlad Seryakov

QA and support:
 Mark Ryland
 Ben Butler

