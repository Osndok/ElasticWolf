<?xml version="1.0"?>
<?xml-stylesheet href="chrome://global/skin/" type="text/css"?>
<!DOCTYPE window SYSTEM "chrome://ec2ui/locale/ec2ui.dtd">

<dialog id="ec2ui.dialog.createvpc"
    xmlns="http://www.mozilla.org/keymaster/gatekeeper/there.is.only.xul"
    buttons="accept,cancel"
    ondialogaccept="return createRoute();"
    ondialogcancel="return true;" onload="init();"
    title="Create a new Route" buttonlabelaccept="Create Route">

    <script type="application/x-javascript" src="chrome://ec2ui/content/model.js" />
    <script>
    function createRoute() {
        this.retVal.cidr = document.getElementById("ec2ui.route.cidr").value.trim();
        if (this.retVal.cidr == "") {
            alert("CIDR must not be empty");
            return false;
        }
        this.retVal.gatewayId = document.getElementById("ec2ui.route.gw").value;
        this.retVal.ok = true;
        return true;
    }

    function init() {
        this.session = window.arguments[0];
        this.retVal = window.arguments[1];

        var menu = document.getElementById("ec2ui.route.gw");
        menu.removeAllItems();
        for (var i = 0; i &lt; retVal.gws.length; i++) {
            menu.appendItem(retVal.gws[i].text, retVal.gws[i].id);
        }
        menu.selectedIndex = 0
    }
    </script>

    <vbox flex="1">
    <groupbox>
    <description>Specify CIDR and choose Internet gateway to create new Route</description>
    <grid flex="1">
        <columns>
            <column />
            <column />
        </columns>
        <rows>
            <row>
                <label control="ec2ui.route.cidr" value="CIDR Block:" />
                <textbox id="ec2ui.route.cidr" size="20" value="" />
            </row>
            <row>
                <label control="ec2ui.route.gw" value="Gateway:" />
                <menulist id="ec2ui.route.gw" />
            </row>
        </rows>
    </grid>
    </groupbox>
</vbox>
</dialog>
